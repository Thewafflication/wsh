# M2 Allocation — WSH-REQ-0070

The canonical requirement is `WSH-REQ-0070`. M2 allocates isolated variables,
diagnostics, limits, allocator, and abstract runtime ownership plus concurrent
execution of separate contexts. Later milestones add the remaining context
state named by the complete requirement.

**Verification:** `TC-0070`
