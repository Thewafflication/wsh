# M8 Allocation — WSH-REQ-0103

A non-C host drives the shared embedding ABI through a foreign-function interface, proving the exported surface is callable from another language when the interpreter architecture matches the target.

**Verification:** `TC-0103`
