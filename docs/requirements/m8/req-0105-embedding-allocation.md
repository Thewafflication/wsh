# M8 Allocation — WSH-REQ-0105

The installed SDK layout of public headers, import library, and shared library is self-contained and lets an external host compile and run against it.

**Verification:** `TC-0105`
