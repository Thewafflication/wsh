# M8 Allocation — WSH-REQ-0073

The executable, static-library host, and shared-library host execute the same
portable-language and embedded-standard-library conformance sources and return
the same ordered process-style result.

**Verification:** `TC-0106`
