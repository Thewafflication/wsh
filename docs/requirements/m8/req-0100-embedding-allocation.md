# M8 Allocation — WSH-REQ-0100

M8 exposes a stable embedding ABI 1 through the static and shared libraries, presenting the same context, value, diagnostic, and version surface, and returning a defined error rather than crashing on documented misuse.

**Verification:** `TC-0100`
