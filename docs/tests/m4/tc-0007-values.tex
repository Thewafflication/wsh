\documentclass[letterpaper]{article}
\usepackage[letterpaper,margin=1in]{geometry}
\input{wsp/testing/test-case-library.tex}
\begin{document}
\def\TCID{TC-0007}
\def\TCTitle{Flat values and variable expansion}
\def\TCPurpose{Verify M4 value semantics for WSH-REQ-0007.}
\def\TCPriority{\TCPriorityHigh}
\def\TCRequirementRef{WSH-REQ-0007}
\def\TCDesignRef{M4 design: Evaluation Model}
\def\TCFeatureRef{Lists, empty values, subscripts, count, flatten}
\def\TCTestTechnique{Equivalence partition and boundary testing.}
\def\TCPreconditions{The evaluator test executable is built.}
\def\TCEnvironment{Deterministic fake runtime.}
\def\TCAssumptions{M3 supplies a complete immutable AST.}
\def\TCInputData{Empty, singleton, multi-element, and subscripted values.}
\def\TCInitialState{A fresh isolated context.}
\def\TCProcedure{Evaluate assignments and launches and inspect requests/state.}
\def\TCExpectedResult{Ordering and empty identity are retained; count,
flatten, and one-origin selections are exact.}
\def\TCPostConditions{All owners are destroyed.}
\TCTemplate
\end{document}
