\documentclass[letterpaper]{article}
\usepackage[letterpaper,margin=1in]{geometry}
\input{wsp/testing/test-case-library.tex}
\begin{document}
\def\TCID{TC-0075}
\def\TCTitle{Hostile portable-core input and global-state isolation}
\def\TCPurpose{Verify applicable WSH-DFS-0001 controls for WSH-REQ-0075.}
\def\TCPriority{\TCPriorityCritical}
\def\TCRequirementRef{WSH-REQ-0075}
\def\TCDesignRef{WSH-DFS-0001 and M2 design DFS update}
\def\TCFeatureRef{Strict decoding, checked limits, contexts, and fake runtime}
\def\TCTestTechnique{Abuse-case, malformed-input, and recovery testing.}
\def\TCPreconditions{Current directory, environment marker, locale, console
code pages, and standard handles can be observed on the Windows host.}
\def\TCEnvironment{Native Windows Debug or Release host.}
\def\TCAssumptions{The test does not mutate those observations itself.}
\def\TCInputData{Malformed encoding corpus and an unrepresentable source size.}
\def\TCInitialState{Global observations are captured before core calls.}
\def\TCProcedure{Reject hostile inputs, execute representative context and
fake-runtime operations, then recapture every process-global observation.}
\def\TCExpectedResult{Every hostile input fails before effect or uncontrolled
allocation, and directory, environment, locale, code pages, and standard
handles are unchanged.}
\def\TCPostConditions{No core object or external effect remains.}
\TCTemplate
\end{document}
