\documentclass[letterpaper]{article}
\usepackage[letterpaper,margin=1in]{geometry}
\input{wsp/testing/test-case-library.tex}
\begin{document}
\def\TCID{TC-0018}
\def\TCTitle{Strict UTF-8 and UTF-16 conversion}
\def\TCPurpose{Verify the portable portion of WSH-REQ-0018.}
\def\TCPriority{\TCPriorityCritical}
\def\TCRequirementRef{WSH-REQ-0018}
\def\TCDesignRef{M2 design: Source and Unicode}
\def\TCFeatureRef{Portable UTF validation and conversion interfaces}
\def\TCTestTechnique{Boundary values and invalid-equivalence partitions.}
\def\TCPreconditions{The portable-core test executable is built.}
\def\TCEnvironment{Any supported C99 host and configuration.}
\def\TCAssumptions{Concrete Win32 calls are outside M2.}
\def\TCInputData{One-, two-, three-, and four-byte scalars; overlong,
truncated, surrogate, out-of-range, NUL, and noncharacter encodings.}
\def\TCInitialState{No owned conversion buffer exists.}
\def\TCProcedure{\begin{enumerate}
\item Validate each partition and count valid scalars.
\item Convert valid UTF-8 to UTF-16 and back.
\item inspect surrogate-pair units and byte-exact round trip.
\end{enumerate}}
\def\TCExpectedResult{Valid text round-trips; every hostile encoding returns
the encoding result without returning a partial buffer.}
\def\TCPostConditions{Returned conversion buffers are allocator-released.}
\TCTemplate
\end{document}
